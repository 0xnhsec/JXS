# JXS — Python dependencies (MVP Phase 1)

# Capture
mitmproxy>=10.0.0

# Extraction
jsbeautifier>=1.14.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Playwright (Phase 2 — Mode A, install manually when needed)
# playwright>=1.44.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0  # for FastAPI TestClient

# Utils
click>=8.1.0  # CLI entry point
rich>=13.0.0  # pretty CLI output
